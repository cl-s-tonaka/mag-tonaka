# 動的エージェント登録アーキテクチャへの移行プラン

## 目標

GitHub PRベースの静的なエージェント生成から、APIを経由した動的エージェント登録アーキテクチャへの移行。

**目的**: 基盤コードを変更せず、再起動不要で、即座に新しいエージェントとツールを追加できるシステムを構築する。

---

## 実現可能性の評価

### ✅ 可能です - ただし難易度は高い

**総合難易度**: **HARD (7/10)**

**判定理由**:
- VoltAgentに`registerAgent()`メソッドが存在し、動的登録がサポートされている
- 既存のLibSQLデータベースインフラが利用可能
- Toolシステムは動的生成に対応
- セキュリティと複雑性の課題がある

### 実装期間

| アプローチ | 期間 | 範囲 |
|-----------|------|------|
| **MVP（概念実証）** | 3-4日 | 基本的な動的エージェント作成と実行 |
| **フル実装** | 2-3週間 | 全機能+セキュリティ+プロダクション対応 |

---

## 現状分析

### 現在のシステム（PR方式）

**ワークフロー**:
```
ユーザー要求
  ↓
AgentGeneratorAgent（コード生成）
  ↓
GitHub PR作成 → マージ → サーバー再起動 ❌
  ↓
新エージェント利用可能
```

**制約**:
- コードベースの変更が必要
- サーバー再起動が必要
- 即座に利用できない
- GitHub アクセスが必要

### 提案するシステム（動的登録）

**ワークフロー**:
```
ユーザー要求
  ↓
AgentGeneratorAgent（定義生成）
  ↓
API経由でDB保存 → voltAgent.registerAgent() ✅
  ↓
即座に利用可能（再起動不要）
```

**利点**:
- コードベース変更不要
- 再起動不要
- 即座に利用可能
- APIドリブン

---

## 実装計画

### MVP実装（3-4日で概念実証）

**目標**: 基本的な動的エージェント作成・永続化・実行を実証

#### Phase 0: インフラ準備（1-2時間）

**タスク**:
1. 依存関係のインストール:
   ```bash
   npm install vm2 @types/vm2
   ```
2. データベーススキーマのマイグレーション作成
3. LibSQL接続テスト

**成果物**: データベーススキーマ、マイグレーションスクリプト

**リスク**: 低

---

#### Phase 1: 基本的な動的エージェント作成（1-2日）

**タスク**:
1. `DynamicAgentManager` サービスの作成
   - `createAgent()` - エージェント定義からAgentインスタンスを生成
   - `loadDynamicAgents()` - 起動時にDBから復元
   - DBへの保存処理
   - VoltAgentへの登録

2. API エンドポイント作成:
   ```
   POST /api/dynamic-agents
   ```

3. `index.ts` の初期化処理修正:
   ```typescript
   const dynamicAgentManager = new DynamicAgentManager(memory, db);
   await dynamicAgentManager.loadDynamicAgents();
   agentService.initialize(agents, dynamicAgentManager);
   ```

4. 再起動時の復元テスト

**成果物**:
- 動的エージェント作成API
- DB永続化
- 再起動時の自動復元

**検証**:
```bash
# エージェント作成
curl -X POST http://localhost:4310/api/dynamic-agents \
  -H "Content-Type: application/json" \
  -d '{
    "agentId": "helloWorld",
    "displayName": "Hello World Agent",
    "description": "Simple test agent",
    "instructions": "あなたは'Hello World'と返すシンプルなエージェントです。",
    "model": "openai/gpt-4o-mini"
  }'

# サーバー再起動
npm run dev

# エージェント一覧確認
curl http://localhost:4310/api/agents-with-examples | jq '.data[] | .id'
# 期待値: "chat", "admin", "agentGenerator", "helloWorld"
```

**リスク**: 中 - VoltAgentの登録APIが想定通り動作しない可能性

---

#### Phase 2: 動的ツール実装（2-3日）

**タスク**:
1. VM2サンドボックスでのツール実行環境構築
2. 動的ツールのコンパイル処理:
   ```typescript
   compileToolImplementation(impl: string, parametersSchema: z.ZodObject)
   ```
3. Toolインスタンスの動的生成
4. エージェントへのツールアタッチ
5. タイムアウト保護（5秒）

**成果物**:
- 動的ツールの実行
- セキュリティサンドボックス
- ツールバリデーション

**検証**:
```bash
# ツール付きエージェント作成
curl -X POST http://localhost:4310/api/dynamic-agents \
  -d '{
    "agentId": "calculator",
    "tools": [{
      "name": "add",
      "description": "Add two numbers",
      "parameters": [
        {"name": "a", "zodType": "number"},
        {"name": "b", "zodType": "number"}
      ],
      "implementation": "return { result: a + b };"
    }]
  }'

# エージェント実行（Phase 4実装後）
curl -X POST http://localhost:4310/api/agents/calculator/execute \
  -d '{"task": "Calculate 5 + 3"}'
```

**リスク**: 高 - セキュリティ脆弱性、実行時エラー

---

#### Phase 3: エージェント実行API（1日）

**タスク**:
1. `/api/agents/:agentId/execute` エンドポイント完成（現在501）
2. DynamicAgentManagerに`executeAgent()`実装
3. VoltAgent Agent実行APIの調査と統合
4. エラーハンドリング

**修正ファイル**: `src/index.ts` (line 106-122)

**成果物**: 完全なエージェント実行API

**検証**:
```bash
# 静的エージェント実行
curl -X POST http://localhost:4310/api/agents/chat/execute \
  -d '{"task": "こんにちは"}'

# 動的エージェント実行
curl -X POST http://localhost:4310/api/agents/helloWorld/execute \
  -d '{"task": "挨拶して"}'
```

**リスク**: 中 - VoltAgent Agent実行APIが不明確

---

### フル実装（2-3週間）

上記MVP + 以下を追加:

#### Phase 4: AgentGeneratorAgent統合（2-3日）

**タスク**:
1. `createDynamicAgent` ツールの追加
2. AgentGeneratorAgentのデュアルモード実装（PR vs 動的）
3. ユーザー選択ワークフロー

**成果物**: PR作成と動的登録の両方をサポート

---

#### Phase 5: 管理API（1-2日）

**タスク**:
```
GET    /api/dynamic-agents           # 一覧
GET    /api/dynamic-agents/:id       # 取得
PUT    /api/dynamic-agents/:id       # 更新
DELETE /api/dynamic-agents/:id       # 削除
POST   /api/dynamic-agents/:id/disable # 無効化
```

**成果物**: CRUD操作、監査ログ

---

#### Phase 6: プロダクション対応（2-3日）

**タスク**:
1. セキュリティ監査
2. レート制限
3. 認証・認可
4. パフォーマンス最適化
5. 監視・ロギング
6. ドキュメント

**成果物**: プロダクション対応システム

---

## アーキテクチャ設計

### データベーススキーマ

```sql
-- 動的エージェント定義
CREATE TABLE dynamic_agents (
  id TEXT PRIMARY KEY,
  class_name TEXT NOT NULL,
  display_name TEXT NOT NULL,
  description TEXT NOT NULL,
  instructions TEXT NOT NULL,
  model TEXT NOT NULL DEFAULT 'openai/gpt-4o-mini',
  status TEXT NOT NULL DEFAULT 'active',
  version INTEGER NOT NULL DEFAULT 1,
  created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
  metadata TEXT
);

-- 動的ツール
CREATE TABLE dynamic_tools (
  id TEXT PRIMARY KEY,
  agent_id TEXT NOT NULL,
  name TEXT NOT NULL,
  description TEXT NOT NULL,
  parameters_schema TEXT NOT NULL,
  implementation TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'active',
  created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (agent_id) REFERENCES dynamic_agents(id) ON DELETE CASCADE
);

-- テスト例
CREATE TABLE dynamic_test_examples (
  id TEXT PRIMARY KEY,
  agent_id TEXT NOT NULL,
  input TEXT NOT NULL,
  description TEXT NOT NULL,
  expected_behavior TEXT NOT NULL,
  created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (agent_id) REFERENCES dynamic_agents(id) ON DELETE CASCADE
);

-- 監査ログ
CREATE TABLE dynamic_agent_audit (
  id TEXT PRIMARY KEY,
  agent_id TEXT NOT NULL,
  operation TEXT NOT NULL,
  user_id TEXT,
  status TEXT NOT NULL,
  error_message TEXT,
  metadata TEXT,
  created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

### DynamicAgentManager サービス

```typescript
// src/services/DynamicAgentManager.ts

class DynamicAgentManager {
  private db: LibSQL;
  private agentInstances: Map<string, Agent> = new Map();
  private memory: Memory;

  async initialize(memory: Memory): Promise<void> {
    this.memory = memory;
    await this.loadDynamicAgents();
  }

  async createAgent(definition: DynamicAgentDefinition): Promise<Agent> {
    // 1. ツールをコンパイル
    const tools = definition.tools.map(t => this.createTool(t));

    // 2. Agentクラスを動的生成
    class DynamicAgent extends Agent {
      constructor(memory: Memory) {
        super({
          name: definition.id,
          instructions: definition.instructions,
          tools,
          model: definition.model,
        });
      }
    }

    // 3. インスタンス化
    const agent = new DynamicAgent(this.memory);

    // 4. VoltAgentに登録
    voltAgent.registerAgent(agent);

    // 5. DB保存
    await this.saveToDatabase(definition);

    // 6. キャッシュ
    this.agentInstances.set(definition.id, agent);

    return agent;
  }

  private createTool(toolDef: DynamicToolDefinition): Tool {
    const executeFn = this.compileToolImplementation(toolDef.implementation);

    return new Tool({
      name: toolDef.name,
      description: toolDef.description,
      parameters: toolDef.parametersSchema,
      execute: executeFn,
    });
  }

  private compileToolImplementation(impl: string): Function {
    // VM2 サンドボックス
    const vm = new VM({
      timeout: 5000,
      sandbox: { fetch, console: logger },
    });

    return vm.run(`(async (params) => { ${impl} })`);
  }

  async loadDynamicAgents(): Promise<void> {
    const agents = await this.db.query(
      'SELECT * FROM dynamic_agents WHERE status = "active"'
    );

    for (const agentData of agents) {
      const tools = await this.loadTools(agentData.id);
      const testExamples = await this.loadTestExamples(agentData.id);

      await this.createAgent({
        ...agentData,
        tools,
        testExamples,
      });
    }
  }
}
```

### AgentService 拡張

```typescript
// src/services/agentService.ts

class AgentService {
  private staticAgents: Record<string, any> = {};
  private dynamicAgentManager: DynamicAgentManager | null = null;

  initialize(
    staticAgents: Record<string, any>,
    dynamicManager: DynamicAgentManager
  ): void {
    this.staticAgents = staticAgents;
    this.dynamicAgentManager = dynamicManager;
  }

  async getAllAgentsInfo() {
    const staticInfo = this.getStaticInfo();
    const dynamicInfo = await this.dynamicAgentManager?.getAllAgents() || [];
    return [...staticInfo, ...dynamicInfo];
  }

  async executeAgent(agentId: string, task: string, context?: string) {
    const agent = await this.getAgent(agentId);
    if (!agent) throw new Error(`Agent not found: ${agentId}`);

    return this.dynamicAgentManager?.executeAgent(agentId, task, context);
  }
}
```

---

## リスク評価

### セキュリティリスク

| リスク | 重要度 | 対策 |
|-------|-------|------|
| **任意コード実行** | 致命的 | VM2サンドボックス、APIホワイトリスト、タイムアウト |
| **リソース枯渇** | 高 | レート制限、タイムアウト、メモリ制限 |
| **インジェクション攻撃** | 高 | 入力バリデーション、Zodスキーマ、パラメータ化クエリ |
| **権限昇格** | 中 | 認証・認可、監査ログ |

**推奨**:
- MVP: 信頼できる内部ユーザーのみ
- プロダクション: Worker Threadsへの移行、厳格な認証

### 後方互換性

| リスク | 影響 | 対策 |
|-------|-----|------|
| **静的エージェントへの影響** | 高 | 静的エージェント登録は変更なし |
| **API破壊的変更** | 中 | 既存エンドポイント維持、新規追加のみ |

### パフォーマンス

| 懸念 | 影響 | 対策 |
|-----|-----|------|
| **起動時間増加** | 中 | 遅延ロード、コンパイル済みツールキャッシュ |
| **メモリ使用量増加** | 中 | 同時動的エージェント数制限、未使用エージェントのアンロード |
| **DB クエリオーバーヘッド** | 低 | インデックス、キャッシング、バッチ操作 |

---

## PR方式 vs 動的方式の比較

| 項目 | PR方式（現在） | 動的方式（提案） |
|-----|-------------|----------------|
| **即座に利用可能** | ❌ 再起動必要 | ✅ 即座 |
| **コードベース変更** | ❌ 必要 | ✅ 不要 |
| **セキュリティ** | ✅ 高（静的コード） | ⚠️ 中（動的実行） |
| **デバッグ** | ✅ 容易 | ⚠️ 困難 |
| **型チェック** | ✅ あり | ❌ なし |
| **コードレビュー** | ✅ あり | ❌ なし |
| **ロールバック** | ✅ git revert | ⚠️ DB削除 |
| **エンドユーザー向け** | ❌ 不向き | ✅ 向いている |

### 共存戦略

**両方をサポート**:
- **静的エージェント**: コア機能（chat, admin, agentGenerator）
- **動的エージェント**: ユーザー作成、実験的機能、一時的なツール

**ワークフロー**:
```
動的エージェント作成
  ↓
本番環境でテスト
  ↓
価値があれば → 静的エージェントに昇格（PR作成）
一時的なら → 完了後削除
```

---

## 重要なファイル

### 作成するファイル

1. **`src/services/DynamicAgentManager.ts`** [新規]
   - 動的エージェントのライフサイクル管理
   - ツールコンパイル
   - DB永続化

2. **`migrations/001_dynamic_agents_schema.sql`** [新規]
   - データベーススキーマ

### 修正するファイル

3. **`src/services/agentService.ts`**
   - 静的+動的エージェントの統合管理
   - `getAllAgentsInfo()` 拡張
   - `executeAgent()` 追加

4. **`src/index.ts`**
   - DynamicAgentManager初期化（line 50付近）
   - エージェント実行API完成（line 106-122）
   - 動的エージェント CRUD API追加

5. **`src/agents/AgentGeneratorAgent_local.ts`** [フル実装時]
   - `createDynamicAgent` ツール追加
   - デュアルモード実装

---

## 検証方法

### MVP検証

**ステップ1: 動的エージェント作成**
```bash
curl -X POST http://localhost:4310/api/dynamic-agents \
  -H "Content-Type: application/json" \
  -d '{
    "agentId": "testAgent",
    "displayName": "Test Agent",
    "description": "Test dynamic agent",
    "instructions": "あなたはテスト用エージェントです。",
    "model": "openai/gpt-4o-mini"
  }'
```

**期待結果**: `{ "success": true, "agentId": "testAgent" }`

**ステップ2: サーバー再起動**
```bash
npm run dev
```

**ステップ3: エージェント一覧確認**
```bash
curl http://localhost:4310/api/agents-with-examples | jq '.data[] | .id'
```

**期待結果**:
```json
["chat", "admin", "agentGenerator", "testAgent"]
```

**ステップ4: エージェント実行**
```bash
curl -X POST http://localhost:4310/api/agents/testAgent/execute \
  -H "Content-Type: application/json" \
  -d '{"task": "こんにちは"}'
```

**期待結果**: エージェントからの応答

---

## まとめ

### 実現可能性

✅ **可能** - VoltAgentの`registerAgent()`メソッドと既存インフラで実装可能

### 難易度

**HARD (7/10)** - セキュリティ、ステート管理、デバッグの複雑性

### 推奨アプローチ

1. **MVPから開始**（3-4日）
   - 基本機能を実証
   - セキュリティは限定的（信頼ユーザーのみ）
   - VM2サンドボックス

2. **成功すればフル実装**（2-3週間）
   - AgentGeneratorAgent統合
   - 管理API
   - プロダクション対応（Worker Threads、認証、監視）

3. **段階的ロールアウト**
   - 内部チームで検証
   - 限定ユーザーに開放
   - 全ユーザーへ展開

### 主要な課題

1. **セキュリティ**: 動的コード実行のリスク → VM2/Worker Threads
2. **デバッグ**: 動的コードのトレースが困難 → 詳細ログ・監視
3. **パフォーマンス**: 多数の動的エージェントのスケーリング → キャッシュ・制限

---

## 実装戦略の比較

### Strategy A: インクリメンタル修正（現状から変更）

**アプローチ**:
- 既存ファイルを修正してDynamicAgentManagerを統合
- memory.dbに新テーブル追加
- AgentGeneratorAgentを拡張
- agentServiceを拡張（静的+動的）

**構造**:
```
既存システム（修正版）
├── index.ts（拡張）
├── agentService.ts（拡張）
├── AgentGeneratorAgent.ts（新ツール追加）
├── memory.db（新テーブル追加）
└── agents/（変更なし）
```

**評価**:
- ✅ **ファイル数が少ない**: 修正5ファイル、追加3ファイル
- ✅ **実装期間が短い**: MVP 5-7日
- ❌ **安全性が低い**: 既存システムへの影響大（4/10）
- ❌ **ロールバックが困難**: コードとDBが混在
- ❌ **テスト分離不可**: 既存機能の回帰テストが必須
- ❌ **メンテナンス困難**: 静的/動的ロジックが混在（3/10）

**リスク**:
1. **DB移行失敗**: memory.dbへのテーブル追加で既存データ破損の可能性
2. **agentService破壊**: 全エージェントの発見機能に影響
3. **起動順序の問題**: DynamicAgentManager初期化タイミング
4. **混在ロジック**: `if (isDynamic)` 分岐がコード全体に

### Strategy B: 0から新規構築（並行システム）

**アプローチ**:
- 完全に独立した`src/dynamic/`ディレクトリを作成
- 新しいdynamic_agents.db（memory.dbと分離）
- 新しいAPI名前空間（/api/v2/dynamic-agents）
- 既存ファイルは変更せず、統合時のみindex.ts修正

**構造**:
```
既存システム              動的システム
├── memory.db             ├── dynamic_agents.db
├── index.ts（変更なし）   ├── dynamicSystem.ts
├── agentService.ts       ├── dynamicAgentService.ts
├── agents/               ├── DynamicAgentCreator.ts
└── API: /api/agents/...  └── API: /api/v2/dynamic-agents/...

統合ポイント（index.ts 3行のみ）:
const dynamicAgents = await dynamicSystem.initialize();
new VoltAgent({ agents: {...existingAgents, ...dynamicAgents} });
```

**評価**:
- ✅ **安全性が高い**: 既存システムへの影響なし（9/10）
- ✅ **簡単なロールバック**: dynamicフォルダ削除のみ
- ✅ **テスト分離**: 独立してテスト可能
- ✅ **メンテナンス容易**: 明確な責任分離（9/10）
- ✅ **段階的デプロイ**: フィーチャーフラグで制御可能
- ❌ **ファイル数が多い**: 追加10ファイル
- ❌ **実装期間やや長い**: MVP 7-10日

**メリット**:
1. **障害分離**: 動的システムのクラッシュが既存エージェントに影響しない
2. **フィーチャーフラグ対応**: 実行時に有効/無効を切り替え可能
3. **段階的ロールアウト**: ステージング → 一部ユーザー → 全体
4. **A/Bテスト可能**: PR方式と動的方式を並行運用して比較
5. **監視分離**: 別DBで独立した監視・ログ

### 総合比較表

| 評価項目 | Strategy A | Strategy B | 勝者 |
|---------|-----------|-----------|------|
| **安全性** | 4/10 | 9/10 | **B** |
| **難易度** | 5/10 | 6/10 | A |
| **MVP期間** | 6/10 | 7/10 | **B** |
| **メンテナンス** | 3/10 | 9/10 | **B** |
| **移行リスク** | 4/10 | 9/10 | **B** |
| **総合** | **22/50** | **40/50** | **B** |

### プロダクション環境での考慮事項

現在のシステムが本番稼働中で、安定性が最優先の場合：

**Strategy A の問題**:
- 単一障害点: agentServiceが壊れると全エージェントが停止
- 困難なロールバック: 以前の安定状態に戻せない
- テストギャップ: 動的エージェントのテストで静的エージェントにリスク
- デプロイリスク: 一括デプロイ、段階的リリース不可

**Strategy B の優位性**:
- **障害分離**: 動的システムのクラッシュが既存に影響しない
- **フィーチャーフラグ**: ランタイムで動的システムを無効化可能
- **段階的ロールアウト**: ステージング → 10%ユーザー → 全体
- **簡単なロールバック**: 動的システム削除、既存エージェント無影響
- **並行運用**: PR方式と動的方式を並べて効果測定

---

## 推奨: Strategy B（0から新規構築）

### 推奨理由

1. **安全性最優先**（最重要）
   - プロダクション環境では安定性が最も重要
   - Strategy Bはリスクを完全に分離
   - 既存エージェントは一切触れない
   - ロールバックは簡単（フォルダ削除のみ）

2. **長期的な投資価値**
   - 明確な責任分離
   - メンテナンスとデバッグが容易
   - 将来の拡張性（バージョニング、A/Bテスト）

3. **許容できるトレードオフ**
   - 開発期間+2-3日: 安定性のためには価値がある
   - ファイル数増加: クリーンなコード構成で相殺
   - コードの重複: 後でライブラリ化して共有可能

4. **プロダクション対応アプローチ**
   - フィーチャーフラグでデプロイ
   - 段階的ロールアウト可能
   - 独立した監視（別DB、ログ）
   - 移行期間中の並行運用

### Strategy B の実装フェーズ

#### Phase 1: 隔離環境で構築（第1週）
```
タスク:
- src/dynamic/ フォルダ構造作成
- DynamicAgentCreator、ストレージ層、マネージャー構築
- 全コンポーネントのユニットテスト
- 独立テスト（統合なし）

成果物:
- 動作する動的エージェントシステム（隔離環境）
- 完全なテストカバレッジ
```

#### Phase 2: 統合（第2週）
```
タスク:
- index.tsへの最小限の統合（3行）
- フィーチャーフラグ追加: ENABLE_DYNAMIC_AGENTS=false
- 統合テスト
- ステージング環境へデプロイ

成果物:
- 統合されたシステム（フラグでOFF）
- 統合テスト完了
```

#### Phase 3: 段階的ロールアウト（第3週）
```
タスク:
- 内部ユーザーに有効化
- メトリクス監視（独立した観測可能性）
- 本番環境に影響を与えずに問題修正
- 検証後、全体にロールアウト

成果物:
- 本番環境で動作する動的エージェントシステム
```

#### Phase 4: 最適化（オプション）
```
タスク:
- 共有コードをライブラリに抽出
- バージョニング、高度な機能追加
- PR方式の廃止を検討

成果物:
- 最適化されたシステム
```

### 重要な実装ファイル（Strategy B）

1. **`src/dynamic/dynamicSystem.ts`** [新規]
   - コア初期化、新システムのメインエントリーポイント

2. **`src/dynamic/storage/dynamicAgentDB.ts`** [新規]
   - dynamic_agents.db用のデータベース層

3. **`src/dynamic/agents/DynamicAgentCreator.ts`** [新規]
   - エージェント作成ロジック（AgentGeneratorAgentパターンを模倣）

4. **`src/index.ts`** [3行のみ修正]
   - 統合ポイント、動的エージェントを静的エージェントにマージ

5. **`src/dynamic/managers/DynamicAgentManager.ts`** [新規]
   - ランタイムエージェントロード、ライフサイクル管理

### ハイブリッドアプローチ（代替案）

もし stakeholder が迅速な提供を希望する場合：

1. **Strategy B アーキテクチャで開始**（独立フォルダ/DB）
2. **フィーチャーフラグをスキップ**（直接デプロイ）
3. **最小限API**（基本的な作成/一覧/削除のみ）
4. **最適化を延期**（バージョニング、A/Bテスト）

これで Strategy B の利点の80%を60%の工数で得られ、隔離による安全性を維持。

---

## 最終判定

**Strategy B（新規構築）を強く推奨**

理由: プロダクション環境の安定性、メンテナンス性、ロールバック能力は、開発期間がやや長くなることを考慮しても投資価値がある。
